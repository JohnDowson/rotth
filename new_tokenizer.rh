include foo::baz::a Har from "bar.rh"

; struct Har do
;     a: u64
;     b: u8
; end


proc hartaker &>Har : u64 do
    drop 0
end

proc main: u64 do
    a cast &>Har hartaker print 0
end

proc c1: u64 do
    a cast &>Har hartaker print 0
end

proc c2: u64 do
    a cast &>Har hartaker print 0
end

proc c3: u64 do
    a cast &>Har hartaker print 0
end
