; include "includee.rh"

; struct Vec do
;     len: u64
;     buf: &>char
; end

; proc foo &>Vec : u64 do
;     -> len @u64
; end

; proc bur do
;     true if 1 else 2 end
; end

const BAR: u64 do 60 9 + end

proc foo: u64 do
    BAR bar
end

proc [T B] bar T : B do
    cast B
end
