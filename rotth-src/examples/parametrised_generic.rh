struct [T] Vec do
    len: u64
    cap: u64
    buf: &>T
end

var FOO: Vec[u64]
var COMPLEX: Vec[Vec[char]]

proc main: u64 do
    var BAR: Vec
    FOO ->buf
    BAR ->buf
    COMPLEX ->buf
    bind f: &>&>u64 b: &>&>u8 c: &>&>Vec[char] do end
    0
end
