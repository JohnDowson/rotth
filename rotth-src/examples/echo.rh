include exit cstrlen puts "../std.rh"
proc main: u64 do
    argc 2 < if 1 exit
    else
        argv 8 std::core::ptr+
        argc 1 - while dup 0 > do
            over cast &>u64  @u64 cast &>char
            dup cstrlen swap puts " " puts
            1 - swap 8 std::core::ptr+ swap
        end drop drop
        "\n" puts
    end
    0
end
