include puts from "../std.rh"

proc main: u64 do
    "hello"
    bind len: u64 str: ptr  do
        1 while dup len <= do
            dup str puts "\n" puts
            1 +
        end drop
    end
    0
end
