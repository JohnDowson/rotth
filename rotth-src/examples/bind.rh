include "./rotth-src/std.rh"

proc main : uint do
    "hello"
    bind len : uint str : ptr  do
        1 while dup len <= do
            dup str puts "\n" puts
            1 +
        end drop
    end
    0
end
